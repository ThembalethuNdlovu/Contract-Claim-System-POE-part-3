<h2>Submit Your Claim</h2>
<form asp-action="SubmitClaim" method="post" enctype="multipart/form-data">
    <div class="claim-form-section">
        <label for="lecturer-name">Lecturer Name:</label>
        <input type="text" id="lecturer-name" name="lecturer" class="form-input" required />
        <label for="hours-worked">Hours Worked:</label>
        <input type="number" id="hours-worked" name="hoursWorked" class="form-input" required />
        <label for="hourly-rate">Hourly Rate:</label>
        <input type="number" id="hourly-rate" name="hourlyRate" class="form-input" required />
        <label for="notes">Notes (Optional):</label>
        <textarea id="notes" name="notes" class="form-input"></textarea>
        <label for="file-upload">Upload Document:</label>
        <input type="file" id="file-upload" name="document" accept=".pdf,.docx,.xlsx" class="form-input" required />
        <small>Accepted file types: .pdf, .docx, .xlsx. Max size 5MB.</small>
        <button type="submit">Submit Your Claim</button>
    </div>
</form>
