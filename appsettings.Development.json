{
  // Logging settings specific to the development environment
  "Logging": {
    "LogLevel": {
      // Default logging level for development
      "Default": "Information",
      // Specialized logging for ASP.NET Core during development
      "Microsoft.AspNetCore": "Warning"
    }
  }
}
