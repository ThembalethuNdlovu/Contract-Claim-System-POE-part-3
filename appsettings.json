{
  // Configuration for logging within the application
  "Logging": {
    "LogLevel": {
      // General logging level for the application
      "Default": "Information",
      // Logging level specific to ASP.NET Core components
      "Microsoft.AspNetCore": "Warning"
    }
  },
  // Define which hosts are allowed to access the application (* allows all)
  "AllowedHosts": "*"
}
